"use client";
import { SideBarFake } from '@repo/ui/components';
import React, { JSX } from "react";

export default function Page(): JSX.Element {
    return (
        <SideBarFake title="Login" />
    );
}
